(function(){
    "use strict";

    // var names = ["Angie", "Cadence", "Gideon", "Arwen"];

    // console.log(names.length);

    // console.log(names[0]);
    // console.log(names[1]);
    // console.log(names[2]);
    // console.log(names[3]);

    // TODO: Create an array of 4 people's names using literal array notation, in a variable called 'names'.

    // TODO: Create a log statement that will log the number of elements in the names array.

    // TODO: Create log statements that will print each of the names array elements individually.

    var noun = prompt("Enter a noun:");
    var noun2 = prompt("Enter another noun:");
    var adj = prompt("Enter an adjetive:");
    var verb = prompt("Enter a verb:");
    var verb2 = prompt("Enter another verb:");

    var selectedWords = [noun, noun2, adj, verb, verb2];
    alert(selectedWords[0] + " and " + selectedWords[1] + " have entered the arena!!!!!! READY!!!!!!! FIGHT!!!!!!!!" + selectedWords[0] + " approacheas " + selectedWords[1] + " with a " + selectedWords[2] + " " + selectedWords[3] + ". But at the last second " + selectedWords[1] + " counters with a " + selectedWords[4] + " and wins the figth!!!!!");



})();



